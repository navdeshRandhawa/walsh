import { ProductFiltersPipe } from './product-filters.pipe';

describe('ProductFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
